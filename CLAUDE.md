# CLAUDE.md

The repository instructions live in [`AGENTS.md`](AGENTS.md) so that every agent
reads the same rules. The import below pulls them into context; if it does not
resolve, read `AGENTS.md` directly before making any change.

@AGENTS.md
