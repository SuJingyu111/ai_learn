# Phase 01 — 第一批算子

结束时你会有：`relu`、`add`、`matmul`、`softmax`，每一个的输出都与你**手算**的
fixture 一致，而且有一个 numpy 对拍脚本作为第二道防线。

这个 phase 建立的习惯比代码更重要：**先手算，再写代码，用手算值断言**。
Phase 04 调 Conv 的时候，这个习惯是你唯一能依靠的东西。

还是不需要 GPU。

从 [step-015](step-015.md) 开始。
